@{
    ViewData["Title"] = "Home Page";
}


<div class="card mt-4">
    <h1 class="card-header">График убытков</h1>

    <div class="card-body">
        <div>
            <canvas id="ABTesting-chart"></canvas>
        </div>
    </div>
</div>

<div>
    <p>На первую кнопку нажимают с шансом в 35%, но из-за того что продукт дешевле, покупка происходит в 60% случаев</p>
    <p>На вторую кнопку нажимают с шансом в 65%, но из-за того что продукт дороже, покупка происходит в 30% случаев</p>
</div>

<table id="num-statistics-table" class="table table-hover">
    <thead class="thead-light">
        <tr>
            <th scope="col">Test Number</th>
            <th scope="col">Users</th>
            <th scope="col">% from Started at least one Basic Lesson</th>
        </tr>
    </thead>
</table>

<input type="hidden" id="calculation-url" value="@ViewData["calculation_url"]" />

@section Scripts
{
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="~/js/site.js"></script>
}